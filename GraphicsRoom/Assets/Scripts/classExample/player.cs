using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class player : Unit
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(unit_health);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
